# Habesha Agenagn V7.8.349

Fixes only the Home Service customer Agree/Decline/Confirm Supabase action.

## Required deployment
1. Replace the deployed `index.html` with this version.
2. Run `supabase/v7_8_349_home_service_customer_agree_fix.sql` once in Supabase SQL Editor.

The new ZIP intentionally contains only the latest updated project file and the one new required SQL file. It does not include previous ZIPs, archived copies, old verification reports, or old SQL versions.
